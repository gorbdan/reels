---
name: smm
description: Контент-завод Сырника. Находит залетевшие нейро-видео-тренды, разбирает механику победителя, делает безопасный ремикс под нашу нишу, монтирует Reels в Remotion на моделях Сырника и собирает день «под ключ» для Instagram-воронки в @SirNikeBot. Запуск — /smm.
tags: [smm, content, instagram, reels, sirnike, neuro-photo, neuro-video, higgsfield, remotion]
version: 1.0-sirnike
language: ru
author: адаптация AGINE SMM v3 под Сырник
---

# СЫРНИК SMM — Контент-завод для Instagram-воронки

> Адаптация скилла «SMM Content Factory v3» под проект Сырник. Базовый процесс сохранён (разведка → разбор → safe-remix → панель → CTA-воронка → продакшен → день под ключ), но переведён на нашу нишу, стек и Windows.

Цель: каждый день собирать готовый контент для **Instagram → трафик в Telegram-бот @SirNikeBot** (нейрофото/нейровидео). Герой — **видео в Seedance**, фото — поддержка.

См. память: [[project-instagram-funnel]], [[project-sirnike]], [[reels-montage-remotion]].

---

## 3 жёстких принципа (не нарушать)

1. **Герой — ВИДЕО (Seedance).** Фото (Nano Banana) — поддержка/карусели. Спина контента — 8 готовых видео-форматов: Переодевашки, Ледяная трансформация, Трансформация с питомцем, Ядерный поцелуй, Падение с крыши, Под водой, Уют и буря, Торт и шампанское.
2. **Честное демо.** Упаковку Reels (хуки, переходы, монтаж, текст) можно делать ЛЮБЫМ инструментом, но сам ДЕМОНСТРИРУЕМЫЙ результат = всегда вывод моделей Сырника (Seedance / Nano Banana 2). Реклама = то, что юзер получит в боте.
3. **Беречь кредиты Higgsfield.** Готовые/библиотечные ассеты переиспользуем бесплатно (`~/Desktop/_webapp/videos/`, `~/Downloads/Telegram Desktop/seedance*.mp4`, 121 result*.jpg). Кредиты тратим ТОЛЬКО на НОВЫЕ форматы/промпты. Всегда preflight `get_cost`; тест Fast/5с (~17.5 кр) → финал 10с (~35 кр) только для победителей. Объёмные ФОТО — в вебе Higgsfield под безлимит (через MCP безлимит не действует).

---

## Стек и окружение (Windows!)

- Python — это **`py`**, НЕ `python` (последний = заглушка Store). Для UTF-8 вывода: `PYTHONUTF8=1 py ...`.
- **ffmpeg** установлен (кадры/экспорт/реврейминг) — работает из git-bash, кириллицу в путях держит.
- **Remotion** для монтажа Reels: Node лежит portable, путь и команды — в [[reels-montage-remotion]]. Проект `C:\reels_studio`. Рендер: `& ".\node_modules\.bin\remotion.cmd" render Summer out.mp4`.
- **Higgsfield MCP** (платный аккаунт pro): `generate_image` (model `nano_banana_2`), `generate_video` (model `seedance_2_0`, mode fast/std), `models_explore`, `presets_show`, `virality_predictor`, `balance`, `media_upload_widget`, `job_display`.
- Whisper установлен (транскрипт референс-Reels). yt-dlp для скачивания референсов.
- Репо: контент/тексты → **gorbdan/reels** (`~/Desktop/reels/`). Готовая выдача Ане → **`~/Desktop/Рилсы/`**, тексты дублировать в `.txt` (utf-8-sig). Видео mp4 — локально (в git не тащим, см. .gitignore).
- Бот: **@SirNikeBot** (https://t.me/SirNikeBot). Валюта «изюминки» 🧀: 1 фото=5 изюм≈25-30₽, видео 5.4-8 изюм/сек. Оффер входа: 5 изюм в подарок + 1 бесплатная генерация/день.

---

## Шаг 1 — Контекст

Прочитать перед стартом: бриф `CLAUDE.md`, `~/Desktop/Рилсы/Контент-план_Сырник_Instagram.xlsx` (календарь + ключ-слова), последний `День_*` для преемственности. Тон бренда — **микс** (дерзко с приколами + спокойно-экспертно по ситуации). ЦА: девушки 18-35, мамы 25-40, эксперты/предприниматели.

---

## Шаг 2 — Разведка трендов (без ScrapeCreators)

ScrapeCreators/n8n у нас нет. Берём тренды из доступного:

- **A. Референс от Ани** — кидает ссылку/описание залетевшего нейро-видео (трансформация, переодевашки, оживление, AI-фотосессия). Самый точный источник.
- **B. WebSearch** — `WebSearch` по «viral AI transformation reels», «nano banana trend», «seedance reels», «нейросеть трансформация рилс тренд». Берём топ-форматы недели.
- **C. Наша библиотека** — 8 Seedance-форматов как кандидаты; оценить потенциал через `virality_predictor` (на готовом mp4).
- **D. Трендовые звуки** — Аня подсказывает актуальные звуки Reels (звук добавляется только в приложении IG).

Если у Reel есть аудио-референс — скачать (yt-dlp) и транскрибировать (whisper, `py`), чтобы разобрать сценарий, а не только подпись.

---

## Шаг 3 — Темы + экспертная панель (B2C-линза)

Показать 3-5 идей под нашу нишу. Затем прогнать через панель из 5 экспертов (каждый — свой узнаваемый угол, под B2C/бьюти):

| Эксперт | Угол |
|---|---|
| **Gary Vaynerchuk** | native-формат IG, attention прямо сейчас |
| **Mr. Beast** | хук и удержание первых 1-2 сек |
| **Alex Hormozi** | оффер/ценность: что юзер уносит (первое фото бесплатно) |
| **Бьюти-SMM** (тренд-эстетика) | сезонность, визуал, «это про меня» для девушек |
| **Честное демо** (наш угол) | совпадает ли результат с тем, что реально умеет бот |

Голосование → консенсус (≥3 голосов). Ответ Ани: `согласен` / `свой выбор` / `пересобрать`.

---

## Шаг 4 — Reference Deconstruction

Разобрать победителя кратко (не вываливать JSON):
- хук (первые 1-2 сек), тип (провокация / трансформация / реврил «фото→видео» / лист);
- структура побитово (для видео — по секундам);
- CTA-тип;
- что НЕ ломаем (хук-механика, ритм, обещание);
- что меняем (формулировки, бренд, локализация RU, привязка к фиче бота).

---

## Шаг 5 — Safe Remix (механика без копии)

- **Хук берём из референса без изменения структуры/энергии** — меняем только язык (рус), конкретику и привязываем к формату Сырника.
- Определить, **какой Seedance/Nano-формат** это показывает (что грузить юзеру: фото лица / в полный рост / +парень / +питомец).
- НЕ копировать дословно, НЕ превращать в сухую рекламу, НЕ ломать выигрышную структуру.

---

## Шаг 5.4 — Trigger-CTA воронка (наш механизм ключ-слов)

Ключ-слово под формат (из календаря): ЛЕТО, ОБРАЗЫ, ЛЁД, ПИТОМЕЦ, ПОЦЕЛУЙ, ОКЕАН, ТРЕВЕЛ, ДР, ФЭШН, ГАЙД, СТАРТ… Уникальное слово на каждый Reels = аттрибуция.

**Правила CTA (из оригинала, оставляем):**
- Голос **от первого лица**, всегда «**пришлю**».
- НЕ упоминать бота/автоответчик/канал/механику. Юзер пишет слово → получает ссылку в ЛС.
- 1 строка, 5-7 слов, слово в CAPS/«кавычках».
- Один триггер на пост. Без upsell/«подпишись».

**Где:**
- Reels — overlay-текст в последние 2-3 сек («напиши ЛЕТО в комментариях») + та же строка в подписи. **На экране ссылку/@бота НЕ показывать** (путает — будто комменты бота).
- Карусель — на payoff-слайде + в подписи.
- Stories — стикер-ссылка `https://t.me/SirNikeBot` (тут ссылка уместна).
- Автоответ в ЛС (ManyChat/SaleBot) на ключ-слово → текст от 1 лица + `https://t.me/SirNikeBot` + оффер (5 изюм в подарок).

Залогировать в `creative_brief.json` (word, landing=t.me/SirNikeBot, applied_to).

---

## Шаг 6 — Продакшен визуала

### Видео-Reels (герой)
1. **Источник клипа:** сначала библиотека (бесплатно) — `_webapp/videos/` (лучшие, но 7/8 в 16:9, кроме ДР.mp4) или `Telegram Desktop/seedance*.mp4`. Если формата нет — генерим НОВЫЙ в Higgsfield (`media_upload_widget` для фото Ани → `generate_video` seedance_2_0, preflight `get_cost`, Fast/5с тест → 10с финал).
2. **Реврейминг 16:9 → 9:16:** ffmpeg блюр-подложка (`scale=...,pad`/boxblur) или кроп под субъект.
3. **Монтаж в Remotion** по паттерну «оживлённое фото» или под формат: полароид/реврил → вспышка → кинетический текст (Montserrat, subsets latin+cyrillic, БЕЗ эмодзи) → CTA-эндкард (акцент-пилюля на ключ-слове, БЕЗ @бота). Детали и гочи — [[reels-montage-remotion]].
4. Экспорт 1080×1920, без звука (трендовый звук Аня кладёт в IG).

### Фото-карусель (гайды/польза)
- Фото-стили генерим в **вебе Higgsfield под безлимит** (я даю промпты из `_webapp/prompt_library.json`), кредиты не тратим.
- Слайды карусели — HTML+Playwright (перекрасить «tech-poster» → бьюти/нейро-эстетика: светлый/тёплый фон, мягкий акцент) ИЛИ простая вёрстка. Размер IG 1080×1350.

---

## Шаг 7 — Сборка дня «под ключ»

В `~/Desktop/Рилсы/День_ГГГГ-ММ-ДД/`:
- `0X_ФИНАЛ_*_1080x1920.mp4` — готовый Reels;
- `День_готовый.md` + `.txt` (utf-8-sig): источник/концепт, монтаж сделан, **подпись + хэштеги**, **закреп-коммент** с ключ-словом, **автоответ** (t.me/SirNikeBot), **5 сторис**, чек-лист публикации, «тебе осталось — трендовый звук».
Тексты продублировать в репо `reels` (видео — локально).

---

## Шаг 8 — Превью/одобрение

Показать Ане карточку дня:
```
ДЕНЬ ГГГГ-ММ-ДД · [формат] · ключ-слово «X»
Хук: ...
Что постим: видео/карусель + сторис
[ок] — финализирую файлы
[правки] — что поправить
```
Без явного `ок` — файлы не финализируем.

---

## Шаг 9 — Публикация (на стороне Ани)

Автопубликации НЕТ (n8n не используем). Аня: открывает финал → **добавляет трендовый звук в IG** (обязательно в приложении) → подпись → закрепляет коммент → настраивает автоответ на ключ-слово → ссылка в шапке → выкладывает Stories → **дублирует в TikTok/YouTube Shorts** (охваты у нового аккаунта).

---

## Режим /smm comments

Черновики ответов на комменты IG/Threads: определить тип (вопрос/сомнение/хейт/лид) → короткий живой ответ в тоне «микс» → если уместно, открытый вопрос. НЕ публиковать, показать черновики Ане: `[ок]/[переписать N]/[стоп]`.

---

## Жёсткие правила (наши)

- Не вызывать Anthropic API напрямую — текст генерится текущим контекстом.
- Видео-генерации — только по явному ОК Ани (тратят кредиты). Всегда `get_cost` сначала.
- Стартовый аккаунт без аудитории → первые 2 недели это ТЕСТ форматов, не ждать вирус; копить 15-20 роликов, читать сигнал (досмотры/сохранения/переходы по ключ-словам), потом усиливать победителей.
- Выдача Ане — в `Рилсы/`, тексты в `.txt` (utf-8-sig): `.md`/без-BOM у неё не открываются (часто на телефоне).
- Прайс — в рублях, источник правды `sirnike/config.py`.

---

## Ключ-слова (справочник из календаря)

ЛЕТО, ОБРАЗЫ, ЛЁД, ПИТОМЕЦ, ПОЦЕЛУЙ, ДРАМА, ОКЕАН, ТРЕВЕЛ, ДР, ФЭШН, МОРЕ, СКАЗКА, ТРАНС, ГАЙД, ПРАВИЛА, ТОП, ЦЕНА, ФЕЙЛ, ПРОФЕССИИ, ЭПОХА, УГАДАЙ, СТАРТ, ПАКЕТ, КОНТЕНТ, ДРУГ. Новое слово — на каждый Reels.

---

## Troubleshooting (Windows)

- `python: command not found` → используй `py`.
- node/npm не видно → задать PATH к portable Node (см. [[reels-montage-remotion]]), запускать в PowerShell.
- `npx remotion add` падает → ставить пакеты `npm install "@remotion/x@<версия remotion>"`.
- ffmpeg drawtext «квадратики» кириллицы → шрифт `C:/Windows/Fonts/arialbd.ttf` + textfile UTF-8; ИЛИ монтаж в Remotion (надёжнее, эмодзи всё равно не рендерить).
- Higgsfield «free/мало кредитов» → проверить `balance`/`list_workspaces`; платный аккаунт мог отвалиться от MCP (переподключить коннектор).
